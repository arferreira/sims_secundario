<%= form_for(@acesso) do |f| %>
  <% if @acesso.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@acesso.errors.count, "error") %> prohibited this acesso from being saved:</h2>

      <ul>
      <% @acesso.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :cracha %><br>
    <%= f.text_field :cracha %>
  </div>
  <div class="field">
    <%= f.label :data %><br>
    <%= f.date_select :data %>
  </div>
  <div class="field">
    <%= f.label :ip %><br>
    <%= f.text_field :ip %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

